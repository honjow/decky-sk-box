export enum SleepMode {
    SUSPEND = "suspend",
    HIBERNATE = "hibernate",
    SUSPEND_THEN_HIBERNATE = "suspend-then-hibernate",
}

export enum SessionMode {
    WAYLAND = "wayland",
    XORG = "xorg",
}